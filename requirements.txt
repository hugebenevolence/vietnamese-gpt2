# Vietnamese GPT-2 Continual Pre-training
# ========================================

# Core ML frameworks
torch>=2.0.0
transformers>=4.35.0
datasets>=2.15.0
tokenizers>=0.15.0

# Training utilities
accelerate>=0.25.0
tensorboard>=2.15.0
wandb>=0.16.0

# Optional: For faster data processing
multiprocess>=0.70.15
